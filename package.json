{
  "name": "xnb2cpweb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch src:src",
    "build:sass": "sass src:src",
    "build:go": "cd ./src/go && bun ./build.sh && cd ../..",
    "decache-css": "bun ./css-cache-bypass.sh",
    "build": "npm run build:sass && rm ./src/index.css.map && npm run decache-css && npm run copy-wasm-exec && npm run build:go",
    "copy-wasm-exec": "bun exec \"cp \\\"$(go env GOROOT)\\\"/misc/wasm/wasm_exec.js ./src/go\""
  },
  "keywords": [],
  "author": "AnotherPillow",
  "license": "GPL-3.0",
  "devDependencies": {
    "sass": "^1.69.5"
  }
}
